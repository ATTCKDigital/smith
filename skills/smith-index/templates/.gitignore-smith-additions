# Smith manifest system — selective gitignore additions.
# Merged into the project's root .gitignore by /smith init.
#
# Per Q5 (spec.md Requirement 1): the per-file .meta sidecars and the
# per-system manifests churn on every commit and are not version-controlled.
# The top-level manifest.md and the config/ directory ARE version-controlled
# (team-shared overview + per-skill config).

.smith/index/files/
.smith/index/systems/
.smith/index/.smith-index-checkpoint.json

# NOT gitignored (commit these):
#   .smith/index/manifest.md
#   .smith/index/config/context-manifest.json
#   .smith/index/config/system-paths.json   (if present)
